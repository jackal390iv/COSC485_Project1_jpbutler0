
public class COSC485_Project1 {

    public static void main(String[] args) {

        TheScanner the_Scanner = new TheScanner();

        the_Scanner.userMessage();



        /*
        String element = "a";
        String connection = "q01";
        
        String temp = element.concat(connection);
        
        System.out.println("element = " + element);
        System.out.println("connection = " + connection);
        System.out.println("temp = " + temp);
        
        String tempmod=temp.substring(1);        
        System.out.println("tempMod = " + tempmod);
        
        String tempextra = temp.substring(0, 1);
        System.out.println("tempExtra = " + tempextra);
         */

        //WARNING: in order for the above to work ^ all elements nmust be one character long..

    }
}
